{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://signal-check.dev/schemas/scored_result.schema.json",
  "title": "Classified result",
  "description": "A structured classification against anchors. It is not an absolute score, a grade, a validation result, or a purchase-probability estimate.",
  "type": "object",
  "required": [
    "segment_id",
    "stimulus_id",
    "dimension_id",
    "anchor",
    "evidence",
    "counter_evidence",
    "uncertainty",
    "confidence_note",
    "interpretation_limit"
  ],
  "properties": {
    "segment_id": { "type": "string" },
    "variant_id": { "type": "string" },
    "stimulus_id": { "type": "string" },
    "dimension_id": { "type": "string" },
    "anchor": { "type": "string" },
    "evidence": { "type": "string" },
    "counter_evidence": { "type": "string" },
    "uncertainty": { "type": "string" },
    "confidence_note": {
      "type": "string",
      "description": "Plain-language confidence note, not a numeric probability."
    },
    "negative_control_relevance": {
      "type": "string",
      "description": "How negative controls affect confidence in this classification."
    },
    "interpretation_limit": {
      "type": "string",
      "description": "What this result must not be used to claim."
    },
    "reviewer_note": { "type": "string" }
  },
  "additionalProperties": false
}
